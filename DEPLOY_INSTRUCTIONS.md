# طريقة رفع الموقع (Deploy) 🚀

أسهل طريقة لرفع موقع React هي استخدام خدمة **Netlify**.

### الخطوة 1: بناء المشروع (Build)
1. افتح الـ CMD في مجلد المشروع.
2. اكتب الأمر التالي:
   ```bash
   npm run build
   ```
3. هذا الأمر سينشئ مجلداً جديداً اسمه `dist` يحتوي على ملفات الموقع جاهزة للرفع.

### الخطوة 2: الرفع على Netlify (بدون حساب GitHub)
1. اذهب إلى موقع [Netlify Drop](https://app.netlify.com/drop).
2. قم بتسجيل الدخول (أو إنشاء حساب مجاني).
3. بعد الدخول، ستجد منطقة مكتوب عليها "Drag and drop your site output folder here".
4. **اسحب** مجلد `dist` الذي ظهر عندك في الخطوة الأولى وأفلته داخل المربع في المتصفح.
5. انتظر ثواني... ومبروك! سيعطيك رابطاً للموقع يعمل على الإنترنت.

### ملاحظات هامة للرفع
1. **مفتاح الذكاء الاصطناعي**: في الاستضافة المجانية، قد تحتاج لإضافة `API_KEY` في إعدادات Netlify.
   - اذهب لـ Site settings > Environment variables.
   - أضف `API_KEY` وضع القيمة الخاصة بك.
2. **إعادة التوجيه (Routing)**:
   - بما أننا نستخدم `HashRouter` في الكود، فالموقع سيعمل مباشرة بدون مشاكل في التنقل بين الصفحات.

بالتوفيق يا بطل! 🌱